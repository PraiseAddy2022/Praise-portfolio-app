name: My first Pipeline

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout codes
        uses: actions/checkout@v4
      - name: Build image
        run: docker build -t PraiseAddy2022/ciportfolio .
      - name: list images
        run: docker images  

     
